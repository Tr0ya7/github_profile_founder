export type user = { avatar_url: string; login: string; location: string; followers: number; following: number }
export type repositores = { name: string; html_url: string; language: string; stargazers_count: number; forks_count: number }